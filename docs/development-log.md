# 岭潮共创开发日志

## 2026-07-30：文化路线反馈、注册登录与权限闭环

### 本轮分支

- 分支：`codex/fix-cultural-route-feedback`
- 基线：`integration/round-1`
- 合并目标：`develop`（通过 Pull Request，不直接推送）

### 已完成

1. 用户注册与登录闭环
   - 新增用户注册页面，复用现有 `/api/v1/auth/register` 契约。
   - 普通登录或注册成功后返回首页；从受限页面进入登录时保留安全的站内回跳地址。
   - 登录状态改为响应式状态，顶部导航即时显示昵称、积分和退出入口。
   - 个人中心、图书馆问答及路线任务互动增加未登录拦截。
2. 管理端用户可见性
   - 新增用户管理页面，读取 `/api/v1/admin/users`。
   - 新注册用户可在管理端列表中查询，不新增数据库字段或迁移。
3. 校园寻迹文化反馈
   - 红棉寻迹后三个地点调整为“何世杰体育馆广场”“校史馆门口”“红色长廊”。
   - 五个任务分别提供醒狮、羊城知识、广州体育、十三行、广州革命记忆主题文化令牌。
   - 图书馆文化问答拆分为独立页面，完成全部题目后均可获得令牌，分数仅用于反馈。
   - 图片任务提交前必须先取得后端上传接口返回的真实资源 ID；重复完成任务不重复加分。

### 验证记录

- 后端 Python `compileall`：通过。
- 后端测试：44 项全部通过。
- `frontend-user` TypeScript 检查与生产构建：通过。
- `frontend-admin` TypeScript 检查与生产构建：通过；存在主包大于 500 kB 的非阻塞分包警告。
- `git diff --check`：通过。
- 已验证真实注册用户能够经后端写入 MySQL，并出现在管理端用户列表。

### 当前边界与已知事项

- 图片打卡当前验证“已通过上传接口生成图片资源”，尚未实现 AI 图像内容识别或地标视觉识别。
- 地点名称和演示任务文案由幂等种子脚本更新；未新增数据库迁移。
- 不在前端保存高德地图或 AI 服务密钥；需要密钥的能力必须由后端代理。
- 本分支通过集中回归并合入 `develop` 前，其他成员不要复制或重复实现本轮公共登录、任务和路由改动。

## 五名成员同步新任务

所有成员统一前置操作：等待本分支 Pull Request 合入 `develop`，然后执行：

```bash
git fetch origin
git switch develop
git pull --ff-only origin develop
```

| 成员 | 建议分支 | 同步任务 | 交付与验收 | 角色1审核点 |
|---|---|---|---|---|
| 角色1：项目负责人/后端架构 | `fix/member1-integration-regression` | 审核本轮 API 契约和种子数据；使用真实 MySQL 回归注册、登录、任务幂等、积分和管理员用户列表；复核 CORS 与演示脚本 | 真实 HTTP 回归记录、无新迁移、工作树干净 | 数据库、公共 API、鉴权与部署配置 |
| 成员2：用户端 | `feature/member2-auth-route-ux` | 验收登录/注册回跳、顶部用户态、退出、移动端导航；补齐未登录点击打卡/问答/个人中心的统一提示；不得改 API 路径 | 用户端类型检查与构建通过；提供游客、注册、登录、退出四段截图或录屏 | 公共路由、请求封装和跨模块页面冲突 |
| 成员3：AI能力 | `feature/member3-cultural-ai-feedback` | 对接文化问答和生成能力的后端适配层；为令牌后的 AI 共创入口提供稳定接口；API Key 只放后端环境变量 | 无前端密钥；失败可降级；接口遵守统一响应；提供脱敏调用日志 | 新增环境变量、外部服务超时和 API 契约 |
| 成员4：路线/积分/徽章 | `feature/member4-checkin-verification` | 在现有任务契约内完善图片打卡验证方案，明确定位、二维码或图像识别的可演示等级；核对五个文化令牌与积分、徽章衔接 | 重复任务不重复加分；上传资源归属校验；目标测试与真实 HTTP 证据 | 任何模型、字段、迁移、任务完成规则变化 |
| 成员5：社区/管理端 | `feature/member5-admin-user-community-qa` | 验收管理端用户列表、分页和权限失败态；确认新用户发布社区内容后的管理端可见性；保持社区模块职责边界 | 管理端类型检查与构建通过；普通用户访问管理接口被拒绝；提供管理员验收截图 | 管理权限、公共管理员接口和与用户端的冲突文件 |

### 协作约束

- 每人只修改职责目录；公共文件先在群内说明并由对应负责人确认。
- 数据库结构变化必须先建 Issue，由角色1审核并统一生成 Alembic 迁移。
- API 保持 `/api/v1` 前缀和 `{code, message, data, requestId}` 响应结构。
- 每个成员通过个人 feature/fix 分支提交 Pull Request，不直接推送 `main` 或 `develop`。
- 交付时附：变更清单、执行命令、验证结果、未完成项和潜在冲突文件。

## 2026-08-01 AI 问答运行时降级验收

- 基准：`origin/develop` `ad4fc51`，修复分支 `codex/fix-ai-chat-runtime`。
- 向量依赖：`sentence-transformers 5.6.1` 已安装，但配置的模型缓存目录不存在；运行时自动切换本地 Markdown 关键词检索，不触发请求期模型下载。
- 运行模式：实现 `RAG_DEEPSEEK`、`KEYWORD_DEEPSEEK`、`PRESET_FALLBACK`；DeepSeek、向量模型或网络不可用时均返回可理解的本地知识回答，不再返回 500。
- 真实配置：本机 `LLM_PROVIDER=mock` 且未检测到 DeepSeek Key，因此真实外部 DeepSeek 调用未执行，未产生费用。
- 验证：AI 目标测试 34 项通过，Python `compileall`、FastAPI 导入及 OpenAPI 40 路径通过；真实 TCP 五项问答均 HTTP 200、业务 `code=0`，已知问题带来源，知识库外问题明确拒答。
- 边界：未修改前端、数据库、迁移、海报生成或社区发布契约。

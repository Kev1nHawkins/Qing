# 岭潮共创：首批 GitHub Issues

> 本文是待确认的 Issue 清单，不代表这些 Issue 已在 GitHub 创建。每项应从 `develop` 建议的 feature/fix 分支实施，通过 PR 合并回 `develop`；数据库和公共 API 变化须先由成员 1 审核。

## 1. [P0][Frontend] 完成文化探索用户页面

- **负责人**：成员 2
- **建议分支**：`feature/member2-user-frontend`
- **预计截止日期**：2026-07-26
- **背景**：用户端已有文化入口和 API 骨架，但文化详情、状态反馈和比赛展示体验尚未完整。
- **任务范围**：完成文化列表、详情、分类/分页交互、加载/空/错误状态；接入既有 `/api/v1/cultures` 契约；保证移动端基本可用。
- **不做内容**：不修改文化表结构、API 路径和统一响应；不顺带实现 AI、地图或社区完整业务。
- **前置依赖**：成员 1 确认文化 API 和示例数据；成员 5 提供经复核的文化内容。
- **验收标准**：核心页面可导航；401/403/404 和网络失败有明确反馈；类型检查、生产构建通过；不在前端保存密钥。
- **测试证据**：页面截图/录屏、请求与响应示例、类型检查和构建日志、受测浏览器与分辨率。
- **潜在冲突文件**：`frontend-user/src/router/index.ts`、`App.vue`、`services/api.ts`、`styles.css`。

## 2. [P0][AI] 完成文化问答与 Prompt 编排

- **负责人**：成员 3
- **建议分支**：`feature/member3-ai-creation`
- **预计截止日期**：2026-07-26
- **背景**：Creation 模型、模板和状态 API 已有骨架，但当前不调用模型，也没有文化问答和稳定的 Prompt 编排。
- **任务范围**：设计文化问答来源策略；校验模板 `options_schema`；实现 Prompt 编排和可替换 Provider 接口；推进 `PENDING/PROCESSING/SUCCESS/FAILED` 状态；提供成员 2 联调契约。
- **不做内容**：不把 API Key 放入前端或 Git；不私改公共认证、响应、Creation 路径；不代做完整用户端页面。
- **前置依赖**：权威文化资料；成员 1 审核环境变量、异步处理与可能的迁移；成员 2 确认交互字段。
- **验收标准**：缺失参数返回明确错误；状态可追踪；失败重试幂等；Provider 超时和错误不泄密；回答包含来源或明确拒答。
- **测试证据**：脱敏请求/响应、状态流转测试、失败/超时用例、日志脱敏截图、Swagger 契约。
- **潜在冲突文件**：`backend/app/api/creation.py`、`schemas/creation.py`、`models/creation.py`、`data/prompts/`、管理端模板占位路由。

## 3. [P0][Route] 完成地图、打卡与路线进度

- **负责人**：成员 4
- **建议分支**：`feature/member4-route-points`
- **预计截止日期**：2026-07-26
- **背景**：路线、地点、任务、积分和徽章基础 API 已有，重复完成不重复加分已验证；地图展示、真实定位和完整进度尚缺。
- **任务范围**：地图 SDK 展示、定位权限、路线详情/进度、地理围栏、二维码/问答/签到交互、积分和徽章反馈。
- **不做内容**：不私改坐标精度、任务枚举、积分幂等键、数据库结构或公共路径；不扩展社区和 AI。
- **前置依赖**：成员 1 审核地图配置和迁移；成员 2 提供用户端承载页面；地图测试 Key 只能通过环境注入。
- **验收标准**：四类任务有可复现验证；拒绝定位和越界有明确提示；重复开始/完成不重复写入或加分；积分流水一致。
- **测试证据**：位置授权/拒绝录屏、任务请求结果、重复提交证据、积分前后对比、类型检查与构建日志。
- **潜在冲突文件**：`api/route.py`、`task.py`、`location.py`、`points.py`、相关 Schema/Model、`seed.py`、用户端路由。

## 4. [P0][Community] 完成社区与管理后台

- **负责人**：成员 5
- **建议分支**：`feature/member5-admin-community`
- **预计截止日期**：2026-07-26
- **背景**：帖子、评论、点赞和部分管理接口是基础骨架；管理端社区入口仍是占位页，收藏、审核、举报、活动和排行未形成完整能力。
- **任务范围**：完成帖子/评论核心体验、管理端审核队列和必要状态反馈；按确认后的 Issue 分批补足比赛主流程所需能力。
- **不做内容**：不一次性扩展所有运营功能；不修改认证、统一响应或数据库公共基类；不让成员 1 代做完整社区业务。
- **前置依赖**：阅读两份成员 5 交接文档；数据库变化须成员 1 审核；用户端入口与成员 2 协调。
- **验收标准**：鉴权和权限隔离正确；点赞等重复操作幂等；管理端审核有明确结果；两个前端检查与构建通过。
- **测试证据**：用户/管理员双角色请求记录、重复操作计数、管理页面截图、Swagger、类型检查与构建日志。
- **潜在冲突文件**：`backend/app/api/community.py`、`admin.py`、社区 Schema/Model、管理端 `router/index.ts`、`services/api.ts`。

## 5. [P0][Deploy] 补做真实 TCP 与 Docker 联调

- **负责人**：成员 1
- **建议分支**：`fix/member1-deployment`
- **预计截止日期**：2026-07-25
- **背景**：TestClient + 真实 MySQL 已验证，但 Uvicorn 真实 TCP、后端容器和四服务联调受此前环境/网络限制尚未完成。
- **任务范围**：网络恢复后构建后端和双前端镜像；启动四服务；验证 health、docs、页面和至少一个前端真实请求。
- **不做内容**：不执行系统清理、删除数据卷、修改业务需求或用 SQLite 替代真实 MySQL 验收。
- **前置依赖**：Docker Hub 可访问；端口可用；使用无真实密钥的本地 `.env`；保留现有 MySQL 数据卷。
- **验收标准**：MySQL healthy；三个应用容器 running；`/health` 和 `/docs` HTTP 200；两个页面可访问；前端请求后端成功。
- **测试证据**：构建摘要、`docker compose ps`、健康响应、容器日志尾部、浏览器截图和接口状态码。
- **潜在冲突文件**：`docker-compose.yml`、三个 Dockerfile、`entrypoint.sh`、`.env.example`、Vite 代理配置。

## 6. [P0][Content] 补齐至少 12 条文化内容

- **负责人**：成员 5 统筹，成员 2 协作
- **建议分支**：`feature/member5-cultural-content`
- **预计截止日期**：2026-07-26
- **背景**：当前种子只有演示文化条目，比赛展示需要覆盖不同类型的岭南文化并注明来源。
- **任务范围**：整理至少 12 条可展示内容；包含标题、分类、摘要、正文和可核验来源；协调前端图片与展示字段。
- **不做内容**：不复制无授权长文；不随意改表字段；不虚构来源；不在该分支改社区运营功能。
- **前置依赖**：成员 1 确认现有字段；成员 2 确认展示长度；团队确认版权和引用规则。
- **验收标准**：至少 12 条内容可由既有 API 查询；字段完整、来源可核验、分类一致；页面无明显溢出；无敏感或侵权材料。
- **测试证据**：内容清单、来源链接、API 查询截图、页面截图和内容复核记录。
- **潜在冲突文件**：`backend/app/scripts/seed.py`、`data/`、文化管理页和用户端文化页；若改种子需成员 1 Review。

## 7. [P0][Integration] 完整主流程联调

- **负责人**：全员
- **建议分支**：各成员保留自己的 feature 分支，通过 PR 集成到 `develop`
- **预计截止日期**：2026-07-27
- **背景**：各领域已有骨架和部分验证，但比赛主流程尚未以真实浏览器和四服务环境端到端验收。
- **任务范围**：注册/登录 → 文化探索 → 路线任务 → 积分/徽章 → AI 创作 → 社区发布 → 管理审核；建立缺陷清单并完成 P0 修复。
- **不做内容**：7 月 27 日后不新增大模块；不在联调 PR 重构架构、升级主依赖或改变数据库/API 契约。
- **前置依赖**：Issues 1–6 达到可联调状态；部署环境可用；成员 1 提供稳定 `develop` 和测试数据。
- **验收标准**：主流程可连续演示；角色权限正确；关键重复操作幂等；刷新/失败/空状态可恢复；P0 缺陷关闭或有明确降级。
- **测试证据**：完整录屏、逐步 HTTP/页面结果、四服务状态、CI 结果、缺陷回归清单和最终演示脚本。
- **潜在冲突文件**：所有公共路由、API 封装、Compose、环境示例、迁移和种子；冲突由相应公共文件负责人协调。

# 比赛演示日检查清单

## 演示前一天

- [ ] 固定当前 Git提交，停止临场升级 Vue、Vite、Python或数据库依赖
- [ ] 确认离线录屏可播放，覆盖登录、文化、路线、任务、积分和社区基础流程
- [ ] 准备预生成的 AI 结果；现场网络不可用时不直接调用外部密钥服务
- [ ] 备份 MySQL 数据和必要上传文件，并验证备份位置
- [ ] 检查 `.env` 已创建且不在 Git跟踪中
- [ ] 演示账号存在；用户名来自本地 `ADMIN_USERNAME`，密码仅由负责人从本地安全配置提供
- [ ] 不在文档、聊天、截图或投屏中显示密码、JWT、数据库连接凭据

## 开场前30分钟

- [ ] Docker Desktop已启动，`docker info` 成功
- [ ] `lingchao-mysql` 为 healthy
- [ ] 3306、8000、5173、5174 端口没有未知占用
- [ ] Python为3.12.x，`.venv` 可用
- [ ] `pip check` 通过
- [ ] Alembic为预期 head，无待处理结构变更
- [ ] 种子数据可查询；不要临场重复清空或重建数据库
- [ ] `scripts\demo-status.ps1` 没有异常

## 服务验收

- [ ] `/health` 返回 HTTP 200、`code=0`
- [ ] `/docs` 可打开 Swagger UI
- [ ] `/openapi.json` 可解析且路径不少于32条
- [ ] 用户端 `http://127.0.0.1:5173` 可访问
- [ ] 管理端 `http://127.0.0.1:5174` 可访问
- [ ] 浏览器网络面板中的 API 请求走相对 `/api/v1`
- [ ] 没有 CORS、502、数据库连接或权限错误

## 完整主流程

- [ ] 普通用户注册与登录
- [ ] 文化列表与详情
- [ ] 路线列表、详情和开始路线
- [ ] 完成任务并获得积分
- [ ] 重复完成任务不重复加分
- [ ] 创建社区基础帖子
- [ ] 点赞及重复点赞幂等
- [ ] 普通用户无法访问管理接口
- [ ] 管理员可以访问管理看板

## 演示结束

- [ ] 宿主机模式使用 `scripts\demo-stop-host.ps1`
- [ ] Docker模式使用 `scripts\demo-stop-docker.ps1`
- [ ] 不删除 MySQL 容器、镜像或数据卷
- [ ] 保存演示日志与问题记录，但移除其中的敏感信息
- [ ] 再次确认代码、数据库和离线录屏备份完整

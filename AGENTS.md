# Codex 工程约束

始终遵守现有目录和命名规范，不随意修改数据库字段和 API 路径，优先保证可维护性和多人协作。每阶段完成后先总结，再进入下一步。

## 强制规则

- 所有 API 使用 `/api/v1` 前缀，统一响应 `{code, message, data, requestId}`。
- 数据库结构变更先建 Issue，由角色 1 审核并统一生成 Alembic 迁移。
- 不在前端保存密钥或直接调用需密钥的 AI 服务。
- 不向 `main` 或 `develop` 直接推送，所有变更通过 PR。
- 不在 7 月 27 日后新增大模块；优先保证比赛主流程稳定。

